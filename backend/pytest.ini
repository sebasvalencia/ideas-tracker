[pytest]
minversion = 8.0
testpaths = tests
addopts = -q --strict-markers --disable-warnings
pythonpath = .
markers =
    unit: unit tests
    integration: integration tests
    bdd: behavior-driven tests
    mvp: minimum viable scenarios
