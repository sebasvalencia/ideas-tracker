global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts/rules.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

scrape_configs:
  - job_name: ideas-api
    static_configs:
      - targets:
          - backend:8000
